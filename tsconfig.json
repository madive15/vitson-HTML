{
  "compilerOptions": {
    // 컴파일될 JavaScript의 버전을 지정합니다. 'esnext'는 최신 ECMAScript 기능을 사용하도록 합니다.
    "target": "esnext",
    // 모듈 시스템을 지정합니다. 'esnext'는 최신 모듈 표준(예: import/export)을 사용합니다.
    "module": "esnext",
    // 모든 엄격한 타입-체킹 옵션을 활성화합니다 (e.g., noImplicitAny, strictNullChecks 등).
    "strict": true,
    // .tsx 파일의 JSX 코드를 어떻게 처리할지 결정합니다. 'preserve'는 JSX를 그대로 유지하여 다른 도구(예: Babel)가 처리하도록 합니다.
    "jsx": "preserve",
    // 모듈 해석 전략을 'node' 방식으로 설정합니다. (node_modules에서 모듈을 찾음)
    "moduleResolution": "node",
    // TypeScript 프로젝트에서 JavaScript 파일(.js)을 허용합니다.
    "allowJs": true,
    // 선언 파일(*.d.ts)의 타입 검사를 건너뜁니다. 라이브러리 타입 검사를 생략하여 컴파일 속도를 높일 수 있습니다.
    "skipLibCheck": true,
    // CommonJS 모듈을 ES6 모듈처럼 'import' 할 수 있게 해주는 호환성 옵션입니다.
    "esModuleInterop": true,
    // default export가 없는 모듈에서 'import React from "react"'와 같은 구문을 허용합니다.
    "allowSyntheticDefaultImports": true,
    // 파일 이름의 대소문자를 일관되게 사용하도록 강제합니다.
    "forceConsistentCasingInFileNames": true,
    // 클래스 필드를 ECMA-262 표준에 따라 정의합니다.
    "useDefineForClassFields": true,
    // 컴파일된 JavaScript 파일에 대한 소스맵(.map) 파일을 생성합니다. 디버깅에 유용합니다.
    "sourceMap": true,
    // 컴파일에 포함될 라이브러리 파일 목록입니다. 'esnext'는 최신 JS 기능, 'dom'은 브라우저 DOM API를 의미합니다.
    "lib": ["esnext", "dom", "dom.iterable", "scripthost"],
    // 모듈 해석의 기준이 되는 경로를 설정합니다.
    "baseUrl": ".",
    // baseUrl을 기준으로 경로 별칭을 설정합니다.
    "paths": {
      "@/*": ["src/*"] // '@/'를 'src/' 경로의 별칭으로 사용합니다.
    }
  },
  // 컴파일할 파일들이 포함된 디렉토리 목록입니다.
  "include": ["src"],
  // 컴파일에서 제외할 파일 및 디렉토리 목록입니다.
  "exclude": ["node_modules", "dist", "src/app.js"]
}
