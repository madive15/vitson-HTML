<!-- prettier-ignore -->
<%
/**
 * @file breadcrumb.ejs
 * @description 브래드크럼 분기 래퍼
 * @param {string} type - 'list' | 'detail'
 */
%>

<% const _src = srcPath; const _public = publicPath; %>

<!-- prettier-ignore -->
<% if (type === 'list') { %>
  <%- include('./list.ejs',{ items: items }) %>
<% } else if (type === 'detail') { %>
  <%- include('./detail.ejs') %>
<% } %>
