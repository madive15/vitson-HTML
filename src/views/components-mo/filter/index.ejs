<!-- prettier-ignore -->
<%
/**
 * @file filter/index.ejs
 * @description 필터 분기 래퍼
 * @param {string} type - 필터 유형: 'product' (추후 확장)
 */
%>

<% const _src = srcPath; const _public = publicPath; %>

<!-- prettier-ignore -->
<% if (type === 'product') { %>
  <%- include('./product.ejs', { items: typeof items !== 'undefined' ? items : [] }) %>
<% } %>
