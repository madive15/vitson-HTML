<% const _src = srcPath; const _public = publicPath; %>
<!-- prettier-ignore -->
<% const _title = (typeof title !== 'undefined' && title) ? title : '최근 본 상품'; %>

<header class="vm-header header-recent-bar">
  <!-- prettier-ignore -->
  <%- include('./header-button.ejs', { 
    button: 'back' })
  %>

  <p class="vm-header-title" data-header-title><%= _title %></p>

  <div class="vm-header-actions">
    <!-- prettier-ignore -->
    <%- include('./header-button.ejs', { 
      button: 'alarm' })
    %>
    <!-- prettier-ignore -->
    <%- include('./header-button.ejs', { 
      button: 'cart' })
    %>
  </div>
</header>
